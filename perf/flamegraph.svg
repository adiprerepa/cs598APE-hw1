<?xml version="1.0" standalone="no"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg version="1.1" width="2400" height="1366" onload="init(evt)" viewBox="0 0 2400 1366" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
<!-- Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples. -->
<!-- NOTES:  -->
<defs>
	<linearGradient id="background" y1="0" y2="1" x1="0" x2="0" >
		<stop stop-color="#eeeeee" offset="5%" />
		<stop stop-color="#eeeeb0" offset="95%" />
	</linearGradient>
</defs>
<style type="text/css">
	text { font-family:Verdana; font-size:12px; fill:rgb(0,0,0); }
	#search, #ignorecase { opacity:0.1; cursor:pointer; }
	#search:hover, #search.show, #ignorecase:hover, #ignorecase.show { opacity:1; }
	#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
	#title { text-anchor:middle; font-size:17px}
	#unzoom { cursor:pointer; }
	#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
	.hide { display:none; }
	.parent { opacity:0.5; }
</style>
<script type="text/ecmascript">
<![CDATA[
	"use strict";
	var details, searchbtn, unzoombtn, matchedtxt, svg, searching, currentSearchTerm, ignorecase, ignorecaseBtn;
	function init(evt) {
		details = document.getElementById("details").firstChild;
		searchbtn = document.getElementById("search");
		ignorecaseBtn = document.getElementById("ignorecase");
		unzoombtn = document.getElementById("unzoom");
		matchedtxt = document.getElementById("matched");
		svg = document.getElementsByTagName("svg")[0];
		searching = 0;
		currentSearchTerm = null;

		// use GET parameters to restore a flamegraphs state.
		var params = get_params();
		if (params.x && params.y)
			zoom(find_group(document.querySelector('[x="' + params.x + '"][y="' + params.y + '"]')));
                if (params.s) search(params.s);
	}

	// event listeners
	window.addEventListener("click", function(e) {
		var target = find_group(e.target);
		if (target) {
			if (target.nodeName == "a") {
				if (e.ctrlKey === false) return;
				e.preventDefault();
			}
			if (target.classList.contains("parent")) unzoom(true);
			zoom(target);
			if (!document.querySelector('.parent')) {
				// we have basically done a clearzoom so clear the url
				var params = get_params();
				if (params.x) delete params.x;
				if (params.y) delete params.y;
				history.replaceState(null, null, parse_params(params));
				unzoombtn.classList.add("hide");
				return;
			}

			// set parameters for zoom state
			var el = target.querySelector("rect");
			if (el && el.attributes && el.attributes.y && el.attributes._orig_x) {
				var params = get_params()
				params.x = el.attributes._orig_x.value;
				params.y = el.attributes.y.value;
				history.replaceState(null, null, parse_params(params));
			}
		}
		else if (e.target.id == "unzoom") clearzoom();
		else if (e.target.id == "search") search_prompt();
		else if (e.target.id == "ignorecase") toggle_ignorecase();
	}, false)

	// mouse-over for info
	// show
	window.addEventListener("mouseover", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = "Function: " + g_to_text(target);
	}, false)

	// clear
	window.addEventListener("mouseout", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = ' ';
	}, false)

	// ctrl-F for search
	// ctrl-I to toggle case-sensitive search
	window.addEventListener("keydown",function (e) {
		if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
			e.preventDefault();
			search_prompt();
		}
		else if (e.ctrlKey && e.keyCode === 73) {
			e.preventDefault();
			toggle_ignorecase();
		}
	}, false)

	// functions
	function get_params() {
		var params = {};
		var paramsarr = window.location.search.substr(1).split('&');
		for (var i = 0; i < paramsarr.length; ++i) {
			var tmp = paramsarr[i].split("=");
			if (!tmp[0] || !tmp[1]) continue;
			params[tmp[0]]  = decodeURIComponent(tmp[1]);
		}
		return params;
	}
	function parse_params(params) {
		var uri = "?";
		for (var key in params) {
			uri += key + '=' + encodeURIComponent(params[key]) + '&';
		}
		if (uri.slice(-1) == "&")
			uri = uri.substring(0, uri.length - 1);
		if (uri == '?')
			uri = window.location.href.split('?')[0];
		return uri;
	}
	function find_child(node, selector) {
		var children = node.querySelectorAll(selector);
		if (children.length) return children[0];
	}
	function find_group(node) {
		var parent = node.parentElement;
		if (!parent) return;
		if (parent.id == "frames") return node;
		return find_group(parent);
	}
	function orig_save(e, attr, val) {
		if (e.attributes["_orig_" + attr] != undefined) return;
		if (e.attributes[attr] == undefined) return;
		if (val == undefined) val = e.attributes[attr].value;
		e.setAttribute("_orig_" + attr, val);
	}
	function orig_load(e, attr) {
		if (e.attributes["_orig_"+attr] == undefined) return;
		e.attributes[attr].value = e.attributes["_orig_" + attr].value;
		e.removeAttribute("_orig_"+attr);
	}
	function g_to_text(e) {
		var text = find_child(e, "title").firstChild.nodeValue;
		return (text)
	}
	function g_to_func(e) {
		var func = g_to_text(e);
		// if there's any manipulation we want to do to the function
		// name before it's searched, do it here before returning.
		return (func);
	}
	function update_text(e) {
		var r = find_child(e, "rect");
		var t = find_child(e, "text");
		var w = parseFloat(r.attributes.width.value) -3;
		var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
		t.attributes.x.value = parseFloat(r.attributes.x.value) + 3;

		// Smaller than this size won't fit anything
		if (w < 2 * 12 * 0.59) {
			t.textContent = "";
			return;
		}

		t.textContent = txt;
		var sl = t.getSubStringLength(0, txt.length);
		// check if only whitespace or if we can fit the entire string into width w
		if (/^ *$/.test(txt) || sl < w)
			return;

		// this isn't perfect, but gives a good starting point
		// and avoids calling getSubStringLength too often
		var start = Math.floor((w/sl) * txt.length);
		for (var x = start; x > 0; x = x-2) {
			if (t.getSubStringLength(0, x + 2) <= w) {
				t.textContent = txt.substring(0, x) + "..";
				return;
			}
		}
		t.textContent = "";
	}

	// zoom
	function zoom_reset(e) {
		if (e.attributes != undefined) {
			orig_load(e, "x");
			orig_load(e, "width");
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_reset(c[i]);
		}
	}
	function zoom_child(e, x, ratio) {
		if (e.attributes != undefined) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = (parseFloat(e.attributes.x.value) - x - 10) * ratio + 10;
				if (e.tagName == "text")
					e.attributes.x.value = find_child(e.parentNode, "rect[x]").attributes.x.value + 3;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseFloat(e.attributes.width.value) * ratio;
			}
		}

		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_child(c[i], x - 10, ratio);
		}
	}
	function zoom_parent(e) {
		if (e.attributes) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = 10;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseInt(svg.width.baseVal.value) - (10 * 2);
			}
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_parent(c[i]);
		}
	}
	function zoom(node) {
		var attr = find_child(node, "rect").attributes;
		var width = parseFloat(attr.width.value);
		var xmin = parseFloat(attr.x.value);
		var xmax = parseFloat(xmin + width);
		var ymin = parseFloat(attr.y.value);
		var ratio = (svg.width.baseVal.value - 2 * 10) / width;

		// XXX: Workaround for JavaScript float issues (fix me)
		var fudge = 0.0001;

		unzoombtn.classList.remove("hide");

		var el = document.getElementById("frames").children;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var a = find_child(e, "rect").attributes;
			var ex = parseFloat(a.x.value);
			var ew = parseFloat(a.width.value);
			var upstack;
			// Is it an ancestor
			if (0 == 0) {
				upstack = parseFloat(a.y.value) > ymin;
			} else {
				upstack = parseFloat(a.y.value) < ymin;
			}
			if (upstack) {
				// Direct ancestor
				if (ex <= xmin && (ex+ew+fudge) >= xmax) {
					e.classList.add("parent");
					zoom_parent(e);
					update_text(e);
				}
				// not in current path
				else
					e.classList.add("hide");
			}
			// Children maybe
			else {
				// no common path
				if (ex < xmin || ex + fudge >= xmax) {
					e.classList.add("hide");
				}
				else {
					zoom_child(e, xmin, ratio);
					update_text(e);
				}
			}
		}
		search();
	}
	function unzoom(dont_update_text) {
		unzoombtn.classList.add("hide");
		var el = document.getElementById("frames").children;
		for(var i = 0; i < el.length; i++) {
			el[i].classList.remove("parent");
			el[i].classList.remove("hide");
			zoom_reset(el[i]);
			if(!dont_update_text) update_text(el[i]);
		}
		search();
	}
	function clearzoom() {
		unzoom();

		// remove zoom state
		var params = get_params();
		if (params.x) delete params.x;
		if (params.y) delete params.y;
		history.replaceState(null, null, parse_params(params));
	}

	// search
	function toggle_ignorecase() {
		ignorecase = !ignorecase;
		if (ignorecase) {
			ignorecaseBtn.classList.add("show");
		} else {
			ignorecaseBtn.classList.remove("show");
		}
		reset_search();
		search();
	}
	function reset_search() {
		var el = document.querySelectorAll("#frames rect");
		for (var i = 0; i < el.length; i++) {
			orig_load(el[i], "fill")
		}
		var params = get_params();
		delete params.s;
		history.replaceState(null, null, parse_params(params));
	}
	function search_prompt() {
		if (!searching) {
			var term = prompt("Enter a search term (regexp " +
			    "allowed, eg: ^ext4_)"
			    + (ignorecase ? ", ignoring case" : "")
			    + "\nPress Ctrl-i to toggle case sensitivity", "");
			if (term != null) search(term);
		} else {
			reset_search();
			searching = 0;
			currentSearchTerm = null;
			searchbtn.classList.remove("show");
			searchbtn.firstChild.nodeValue = "Search"
			matchedtxt.classList.add("hide");
			matchedtxt.firstChild.nodeValue = ""
		}
	}
	function search(term) {
		if (term) currentSearchTerm = term;
		if (currentSearchTerm === null) return;

		var re = new RegExp(currentSearchTerm, ignorecase ? 'i' : '');
		var el = document.getElementById("frames").children;
		var matches = new Object();
		var maxwidth = 0;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var func = g_to_func(e);
			var rect = find_child(e, "rect");
			if (func == null || rect == null)
				continue;

			// Save max width. Only works as we have a root frame
			var w = parseFloat(rect.attributes.width.value);
			if (w > maxwidth)
				maxwidth = w;

			if (func.match(re)) {
				// highlight
				var x = parseFloat(rect.attributes.x.value);
				orig_save(rect, "fill");
				rect.attributes.fill.value = "rgb(230,0,230)";

				// remember matches
				if (matches[x] == undefined) {
					matches[x] = w;
				} else {
					if (w > matches[x]) {
						// overwrite with parent
						matches[x] = w;
					}
				}
				searching = 1;
			}
		}
		if (!searching)
			return;
		var params = get_params();
		params.s = currentSearchTerm;
		history.replaceState(null, null, parse_params(params));

		searchbtn.classList.add("show");
		searchbtn.firstChild.nodeValue = "Reset Search";

		// calculate percent matched, excluding vertical overlap
		var count = 0;
		var lastx = -1;
		var lastw = 0;
		var keys = Array();
		for (k in matches) {
			if (matches.hasOwnProperty(k))
				keys.push(k);
		}
		// sort the matched frames by their x location
		// ascending, then width descending
		keys.sort(function(a, b){
			return a - b;
		});
		// Step through frames saving only the biggest bottom-up frames
		// thanks to the sort order. This relies on the tree property
		// where children are always smaller than their parents.
		var fudge = 0.0001;	// JavaScript floating point
		for (var k in keys) {
			var x = parseFloat(keys[k]);
			var w = matches[keys[k]];
			if (x >= lastx + lastw - fudge) {
				count += w;
				lastx = x;
				lastw = w;
			}
		}
		// display matched percent
		matchedtxt.classList.remove("hide");
		var pct = 100 * count / maxwidth;
		if (pct != 100) pct = pct.toFixed(1)
		matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
	}
]]>
</script>
<rect x="0.0" y="0" width="2400.0" height="1366.0" fill="url(#background)"  />
<text id="title" x="1200.00" y="24" >Flame Graph</text>
<text id="details" x="10.00" y="1349" > </text>
<text id="unzoom" x="10.00" y="24" class="hide">Reset Zoom</text>
<text id="search" x="2290.00" y="24" >Search</text>
<text id="ignorecase" x="2374.00" y="24" >ic</text>
<text id="matched" x="2290.00" y="1349" > </text>
<g id="frames">
<g >
<title>timekeeping_update (49,308,465 samples, 1.82%)</title><rect x="302.5" y="613" width="43.3" height="47.0" fill="rgb(238,152,36)" rx="2" ry="2" />
<text  x="305.47" y="639.5" >time..</text>
</g>
<g >
<title>Vector::operator- (49,081,796 samples, 1.81%)</title><rect x="1645.4" y="1141" width="43.1" height="47.0" fill="rgb(214,42,10)" rx="2" ry="2" />
<text  x="1648.38" y="1167.5" >Vect..</text>
</g>
<g >
<title>update_wall_time (49,308,465 samples, 1.82%)</title><rect x="302.5" y="709" width="43.3" height="47.0" fill="rgb(207,11,2)" rx="2" ry="2" />
<text  x="305.47" y="735.5" >upda..</text>
</g>
<g >
<title>load_elf_interp.constprop.0 (268,534 samples, 0.01%)</title><rect x="1904.3" y="661" width="0.3" height="47.0" fill="rgb(224,91,21)" rx="2" ry="2" />
<text  x="1907.32" y="687.5" ></text>
</g>
<g >
<title>Vector::dot (1,334,507,466 samples, 49.32%)</title><rect x="345.8" y="1141" width="1173.8" height="47.0" fill="rgb(251,213,51)" rx="2" ry="2" />
<text  x="348.84" y="1167.5" >Vector::dot</text>
</g>
<g >
<title>handle_mm_fault (268,534 samples, 0.01%)</title><rect x="1904.3" y="421" width="0.3" height="47.0" fill="rgb(234,135,32)" rx="2" ry="2" />
<text  x="1907.32" y="447.5" ></text>
</g>
<g >
<title>vma_alloc_folio (268,534 samples, 0.01%)</title><rect x="1904.3" y="229" width="0.3" height="47.0" fill="rgb(245,187,44)" rx="2" ry="2" />
<text  x="1907.32" y="255.5" ></text>
</g>
<g >
<title>main.exe (2,705,909,183 samples, 100.00%)</title><rect x="10.0" y="1237" width="2380.0" height="47.0" fill="rgb(216,53,12)" rx="2" ry="2" />
<text  x="13.00" y="1263.5" >main.exe</text>
</g>
<g >
<title>Vector::operator* (96,510,598 samples, 3.57%)</title><rect x="1560.5" y="1141" width="84.9" height="47.0" fill="rgb(233,131,31)" rx="2" ry="2" />
<text  x="1563.49" y="1167.5" >Vector::o..</text>
</g>
<g >
<title>asm_sysvec_apic_timer_interrupt (49,308,465 samples, 1.82%)</title><rect x="302.5" y="1093" width="43.3" height="47.0" fill="rgb(232,127,30)" rx="2" ry="2" />
<text  x="305.47" y="1119.5" >asm_..</text>
</g>
<g >
<title>do_fault (268,534 samples, 0.01%)</title><rect x="1904.3" y="277" width="0.3" height="47.0" fill="rgb(248,200,47)" rx="2" ry="2" />
<text  x="1907.32" y="303.5" ></text>
</g>
<g >
<title>Box::getIntersection (191,767,609 samples, 7.09%)</title><rect x="10.0" y="1141" width="168.7" height="47.0" fill="rgb(217,59,14)" rx="2" ry="2" />
<text  x="13.00" y="1167.5" >Box::getIntersection</text>
</g>
<g >
<title>calcColor (145,969,938 samples, 5.39%)</title><rect x="1775.9" y="1141" width="128.4" height="47.0" fill="rgb(209,19,4)" rx="2" ry="2" />
<text  x="1778.93" y="1167.5" >calcColor</text>
</g>
<g >
<title>__handle_mm_fault (268,534 samples, 0.01%)</title><rect x="1904.3" y="373" width="0.3" height="47.0" fill="rgb(207,9,2)" rx="2" ry="2" />
<text  x="1907.32" y="399.5" ></text>
</g>
<g >
<title>do_execveat_common.isra.0 (268,534 samples, 0.01%)</title><rect x="1904.3" y="949" width="0.3" height="47.0" fill="rgb(231,123,29)" rx="2" ry="2" />
<text  x="1907.32" y="975.5" ></text>
</g>
<g >
<title>tick_do_update_jiffies64 (49,308,465 samples, 1.82%)</title><rect x="302.5" y="757" width="43.3" height="47.0" fill="rgb(208,14,3)" rx="2" ry="2" />
<text  x="305.47" y="783.5" >tick..</text>
</g>
<g >
<title>tick_nohz_highres_handler (49,308,465 samples, 1.82%)</title><rect x="302.5" y="853" width="43.3" height="47.0" fill="rgb(215,50,11)" rx="2" ry="2" />
<text  x="305.47" y="879.5" >tick..</text>
</g>
<g >
<title>__alloc_pages (268,534 samples, 0.01%)</title><rect x="1904.3" y="133" width="0.3" height="47.0" fill="rgb(233,129,30)" rx="2" ry="2" />
<text  x="1907.32" y="159.5" ></text>
</g>
<g >
<title>search_binary_handler (268,534 samples, 0.01%)</title><rect x="1904.3" y="757" width="0.3" height="47.0" fill="rgb(208,14,3)" rx="2" ry="2" />
<text  x="1907.32" y="783.5" ></text>
</g>
<g >
<title>get_page_from_freelist (268,534 samples, 0.01%)</title><rect x="1904.3" y="85" width="0.3" height="47.0" fill="rgb(252,218,52)" rx="2" ry="2" />
<text  x="1907.32" y="111.5" ></text>
</g>
<g >
<title>load_elf_binary (268,534 samples, 0.01%)</title><rect x="1904.3" y="709" width="0.3" height="47.0" fill="rgb(223,83,20)" rx="2" ry="2" />
<text  x="1907.32" y="735.5" ></text>
</g>
<g >
<title>Vector::mag2 (46,467,556 samples, 1.72%)</title><rect x="1519.6" y="1141" width="40.9" height="47.0" fill="rgb(244,182,43)" rx="2" ry="2" />
<text  x="1522.62" y="1167.5" >Vec..</text>
</g>
<g >
<title>__sysvec_apic_timer_interrupt (49,308,465 samples, 1.82%)</title><rect x="302.5" y="997" width="43.3" height="47.0" fill="rgb(242,173,41)" rx="2" ry="2" />
<text  x="305.47" y="1023.5" >__sy..</text>
</g>
<g >
<title>[unknown]  (99,348,803 samples, 3.67%)</title><rect x="1688.5" y="1141" width="87.4" height="47.0" fill="rgb(253,223,53)" rx="2" ry="2" />
<text  x="1691.55" y="1167.5" >[unknown] </text>
</g>
<g >
<title>do_syscall_64 (268,534 samples, 0.01%)</title><rect x="1904.3" y="1093" width="0.3" height="47.0" fill="rgb(209,20,4)" rx="2" ry="2" />
<text  x="1907.32" y="1119.5" ></text>
</g>
<g >
<title>tick_sched_do_timer (49,308,465 samples, 1.82%)</title><rect x="302.5" y="805" width="43.3" height="47.0" fill="rgb(227,104,25)" rx="2" ry="2" />
<text  x="305.47" y="831.5" >tick..</text>
</g>
<g >
<title>__hrtimer_run_queues (49,308,465 samples, 1.82%)</title><rect x="302.5" y="901" width="43.3" height="47.0" fill="rgb(237,150,35)" rx="2" ry="2" />
<text  x="305.47" y="927.5" >__hr..</text>
</g>
<g >
<title>handle_pte_fault (268,534 samples, 0.01%)</title><rect x="1904.3" y="325" width="0.3" height="47.0" fill="rgb(217,57,13)" rx="2" ry="2" />
<text  x="1907.32" y="351.5" ></text>
</g>
<g >
<title>exec_binprm (268,534 samples, 0.01%)</title><rect x="1904.3" y="805" width="0.3" height="47.0" fill="rgb(244,180,43)" rx="2" ry="2" />
<text  x="1907.32" y="831.5" ></text>
</g>
<g >
<title>hrtimer_interrupt (49,308,465 samples, 1.82%)</title><rect x="302.5" y="949" width="43.3" height="47.0" fill="rgb(228,109,26)" rx="2" ry="2" />
<text  x="305.47" y="975.5" >hrti..</text>
</g>
<g >
<title>getLight (515,131,678 samples, 19.04%)</title><rect x="1936.9" y="1141" width="453.1" height="47.0" fill="rgb(240,163,39)" rx="2" ry="2" />
<text  x="1939.91" y="1167.5" >getLight</text>
</g>
<g >
<title>fix (36,789,415 samples, 1.36%)</title><rect x="1904.6" y="1141" width="32.3" height="47.0" fill="rgb(251,212,50)" rx="2" ry="2" />
<text  x="1907.55" y="1167.5" >fix</text>
</g>
<g >
<title>bprm_execve.part.0 (268,534 samples, 0.01%)</title><rect x="1904.3" y="853" width="0.3" height="47.0" fill="rgb(228,106,25)" rx="2" ry="2" />
<text  x="1907.32" y="879.5" ></text>
</g>
<g >
<title>x64_sys_call (268,534 samples, 0.01%)</title><rect x="1904.3" y="1045" width="0.3" height="47.0" fill="rgb(247,194,46)" rx="2" ry="2" />
<text  x="1907.32" y="1071.5" ></text>
</g>
<g >
<title>[unknown] (2,705,909,183 samples, 100.00%)</title><rect x="10.0" y="1189" width="2380.0" height="47.0" fill="rgb(210,24,5)" rx="2" ry="2" />
<text  x="13.00" y="1215.5" >[unknown]</text>
</g>
<g >
<title>alloc_pages_mpol (268,534 samples, 0.01%)</title><rect x="1904.3" y="181" width="0.3" height="47.0" fill="rgb(254,227,54)" rx="2" ry="2" />
<text  x="1907.32" y="207.5" ></text>
</g>
<g >
<title>all (2,705,910,749 samples, 100%)</title><rect x="10.0" y="1285" width="2380.0" height="47.0" fill="rgb(213,39,9)" rx="2" ry="2" />
<text  x="13.00" y="1311.5" ></text>
</g>
<g >
<title>asm_exc_page_fault (268,534 samples, 0.01%)</title><rect x="1904.3" y="565" width="0.3" height="47.0" fill="rgb(225,93,22)" rx="2" ry="2" />
<text  x="1907.32" y="591.5" ></text>
</g>
<g >
<title>elf_load (268,534 samples, 0.01%)</title><rect x="1904.3" y="613" width="0.3" height="47.0" fill="rgb(253,223,53)" rx="2" ry="2" />
<text  x="1907.32" y="639.5" ></text>
</g>
<g >
<title>clear_page_erms (268,534 samples, 0.01%)</title><rect x="1904.3" y="37" width="0.3" height="47.0" fill="rgb(229,113,27)" rx="2" ry="2" />
<text  x="1907.32" y="63.5" ></text>
</g>
<g >
<title>__x64_sys_execve (268,534 samples, 0.01%)</title><rect x="1904.3" y="997" width="0.3" height="47.0" fill="rgb(247,193,46)" rx="2" ry="2" />
<text  x="1907.32" y="1023.5" ></text>
</g>
<g >
<title>timekeeping_advance (49,308,465 samples, 1.82%)</title><rect x="302.5" y="661" width="43.3" height="47.0" fill="rgb(227,104,25)" rx="2" ry="2" />
<text  x="305.47" y="687.5" >time..</text>
</g>
<g >
<title>sysvec_apic_timer_interrupt (49,308,465 samples, 1.82%)</title><rect x="302.5" y="1045" width="43.3" height="47.0" fill="rgb(220,69,16)" rx="2" ry="2" />
<text  x="305.47" y="1071.5" >sysv..</text>
</g>
<g >
<title>bprm_execve (268,534 samples, 0.01%)</title><rect x="1904.3" y="901" width="0.3" height="47.0" fill="rgb(218,61,14)" rx="2" ry="2" />
<text  x="1907.32" y="927.5" ></text>
</g>
<g >
<title>entry_SYSCALL_64_after_hwframe (268,534 samples, 0.01%)</title><rect x="1904.3" y="1141" width="0.3" height="47.0" fill="rgb(218,63,15)" rx="2" ry="2" />
<text  x="1907.32" y="1167.5" ></text>
</g>
<g >
<title>do_user_addr_fault (268,534 samples, 0.01%)</title><rect x="1904.3" y="469" width="0.3" height="47.0" fill="rgb(228,108,25)" rx="2" ry="2" />
<text  x="1907.32" y="495.5" ></text>
</g>
<g >
<title>exc_page_fault (268,534 samples, 0.01%)</title><rect x="1904.3" y="517" width="0.3" height="47.0" fill="rgb(212,35,8)" rx="2" ry="2" />
<text  x="1907.32" y="543.5" ></text>
</g>
<g >
<title>Box::getLightIntersection (190,065,790 samples, 7.02%)</title><rect x="178.7" y="1141" width="167.1" height="47.0" fill="rgb(212,32,7)" rx="2" ry="2" />
<text  x="181.67" y="1167.5" >Box::getLightIntersec..</text>
</g>
</g>
</svg>
